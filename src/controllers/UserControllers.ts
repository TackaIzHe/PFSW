import { NextFunction, Request, Response } from "express";

export class UserControllers{
    static getAll(req:Request, res:Response, next:NextFunction){

    }

    static getUser(req:Request, res:Response, next:NextFunction){
        
    }

    static postUser(req:Request, res:Response, next:NextFunction){
        
    }

    static putUser(req:Request, res:Response, next:NextFunction){
        
    }

    static delUser(req:Request, res:Response, next:NextFunction){
        
    }

    static uploadImg(req:Request, res:Response, next:NextFunction){
        
    }

    static updateImg(req:Request, res:Response, next:NextFunction){
        
    }
}